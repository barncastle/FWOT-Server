# One line ending everywhere: this repo is built on Windows and consumed on
# Linux hosts. Git normalises to LF in the object store and checks out LF.
* text=auto eol=lf

# The new-player template is a protobuf blob -- never touch its bytes.
*.pb binary
